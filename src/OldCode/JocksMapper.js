import React from "react"

function JockMapper(props){

    return(
        <div>
            <h1>id = {props.id}</h1>
            <h1>question = {props.question}</h1>
            <h1>answer = {props.answer}</h1>
        </div>

    )
}

export default JockMapper

